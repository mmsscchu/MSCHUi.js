export default class Grid{
    private gridName : string
    private gridOption : any

    constructor(gridName : string, gridOption : any) {
        this.gridName = gridName
        this.gridOption = gridOption
    }
}